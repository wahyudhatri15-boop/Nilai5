# SiGrade Server Configuration
PORT=3000

# Kredensial Supabase di-hardcode di:
#   - supabaseClient.js (browser)
#   - lib/supabaseConfig.js (server)
#
# Untuk auto-sync server → Supabase, isi SUPABASE_SERVICE_ROLE_KEY
# di lib/supabaseConfig.js (Dashboard → Settings → API → service_role)